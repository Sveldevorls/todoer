export type TodoObject = {
  id: string;
  title: string;
  description: string;
  notes: string;
  group: string;
  isCompleted: boolean;
};

export type GroupObject = {
  id: string;
  title: string;
};
